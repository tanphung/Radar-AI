name: Monitor coins

on:
  schedule:
    # Every 4 hours. Each scheduled run submits one monitor_batch tx for 10 coins.
    - cron: "0 */4 * * *"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: monitor-coins
  cancel-in-progress: false

jobs:
  monitor:
    name: monitor_batch x1 curated-10
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install cron deps
        working-directory: cron
        run: npm install --no-audit --no-fund

      - name: Run monitor
        working-directory: cron
        env:
          GENLAYER_NETWORK: ${{ secrets.GENLAYER_NETWORK || 'testnet-bradbury' }}
          CONTRACT_ADDRESS: ${{ secrets.CONTRACT_ADDRESS }}
          CRON_PRIVATE_KEY: ${{ secrets.CRON_PRIVATE_KEY }}
        run: npm run monitor
